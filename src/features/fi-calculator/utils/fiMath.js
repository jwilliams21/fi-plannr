

export const basicInvestmentCalc = ({ startingAmt, duration, rate, addContribute }) => {
    console.log({ startingAmt, duration, rate, addContribute });

    // Make this function do one thing: calculate the baseline trajectory array based on the numbers it is given.  Keep the data raw (no formatting currencies, creating strings, etc.)
};